<p>Witaj <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p>Kontaktujemy się z tobą, aby powiadomić cię o zmienia adresu email: <%= @resource.unconfirmed_email %>.</p>
<% else %>
  <p>Kontaktujemy się z tobą, aby powiadomić cię o zmianie adresu email: <%= @resource.email %>.</p>
<% end %>
